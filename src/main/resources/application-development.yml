spring:
  datasource:
    url: jdbc:h2:mem:task_manager
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.orm.jpa: DEBUG